
# 컴퓨터 대표적 3개 오류
## systex
## run time(exception)
## semanntic
* 컴퓨터 언어로 표현해야하는데 잘 못 적었을 때 나오는 오류

# 디렉터리 chap2.pdf 내용 6번부터
* 모든 디바이스도 파일로 인식해서 처리한다. (디렉터리도 파일이라 생각함)

## 특별한 디렉터리 몇 가지(외워야 할 것)
```
. - 자신에 대한 정보
.. - 부모에 대한 정보
/ - 모든 디렉터리에 출발점(root), 절대경로에 대해서)(절대 경로와 상대 경로)
~ - 홈 디렉터
```
```
cat > (파일이름).txt 작성 후 컨트롤 + D
cat (파일이름).txt - 그 파일 내용 확인 가능
touch (파일이름).txt - 빈 파일 만듬. 기존에 있는 파일 이름을 쓰면 그 파일의 타임스태프를 바꿈.
```

## 파일은 두 가지로 구분된다.
1. 파일에 대한 정보
2. 실제 파일의 내용

## 경로명
* 절대 경로명 - 루트 디렉터리로부터 시작하여 경로 이름을 정확하게 적는 것
* 상대 경로명 - 현재 작업 디렉터리부터 시작해서 경로 이름을 적는 것

## 디렉터리 리스트 pdf 12

## ** 모든 사용자는 특정한 그룹에 들어가게 되어있다. **
교수님이 말한 개념 다시 잡아놓으라고 한 부분

## head/tail/wc
##  cp , mv , re
* $ rm –r 디렉터리, -r은 리커전 옵션으로 디렉터리 아래의 모든 것을 삭제한다.(무자비하고 위험함..)

## 링크와 하드 링크
* 기존 파일에 대한 새로운 이름이라고 생각할 수 있다.
* 실제로 기존 파일을 대표하는 i-노드를 가리켜 구현한다

## 심볼릭 링크
* 다른 파일을 가리키고 있는 별도의 파일.

## 파일 관련 명령어 pdf 26

## 파일 속성
* 파일의 이름, 타입, 크기, 소유자, 접근권한, 수정 시간

## 접근 권한과 권한 표
## 소유자 및 그룹 변경 : chown, chgrp

## 출력 재지정과 예
* 명령어의 표준출력 내용을 모니터 대신에 파일에 저장한다.
```
$ 명령어 > 파일
$ who > names.txt
$ ls –sl > list.txt
```

## 출력 추가
* 명령어의 표준출력을 모니터 대신에 파일에 추가한다.
* $ 명령어 >> 파일

# chap03.pdf 내용

## 명령어에 대한 설명 
man
info
--help
whatis

# chap3.pdf 내용

## C 프로그램
* C 언어 컴파일 단위는 파일이다.

## 윈도우에서 wsl2 경로 찾기
\\wsl$\
Windows → WSL 경로 접근
